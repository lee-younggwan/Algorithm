백준 lee-younggwan
구글 mystery.younggwan@gmail.com

--------------------------------------------------------------------------------------------
getline(cin, str) // 한줄씩 입력받음, 공백 개행 다 받음
종료조건이 따로 없는 경우 while( ) 안에 입력함수를 넣으면 된다.

--------------------------------------------------------------------------------------------
vector capacity는 메모리가 할당되어 있는 용량
vector size는 실제 유효한 element들...

reserve()는 메모리만 할당..
resize()는 초기화까지

vector값에 push_back 이 아닌 직접 입력을 받고 싶다면 초기화가 되어 있어야함... 메모리할당이 아닌 요소의 초기화가 되어 있어야한다.

--------------------------------------------------------------------------------------------
순서가 보장되는 정렬 stable_sort()

----------------------------------------------
string 형을 int 형으로 atoi(str.c_str()) // atoi의 인자가 char*
stoi 있다.

-----------------------------------------------
입력값을 인접행렬, 인접리스트로 표현...

----------------------------------------------
DP는 최적값을 구할 때 주로 활용

-------------------------------------------
DFS는 완전탐색이 필요한 경우에 주로 사용
완전탐색을 위한 DFS의 시간복잡도는 N!에 근접하는 시간이 긴 알고리즘
지나온 경로를 쉽게 파악할 수 있음

----------------------------------------------
BFS는 그래프나 트리에서 한점을 기준으로 가까운 거리의 정점들부터 거리순으로 탐색하기 때문에 최소 거리 혹은 최소 비용 문제애서 활용
BFS는 거리순으로 정점들을 탐색하기에 지나온 경로를 추적하려면 별도의 처리를 해야함

--------------------------------------------
이분탐색
정렬된 수에서 left, right, mid값을 이용 mid+1, mid-1..
코테에는 잘 안나온다고 한다